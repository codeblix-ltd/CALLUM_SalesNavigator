# Security review

Environment: source review and local tests. Baseline SHA: `68f5971a5e4b91d0814cd0f2b32dd18dbd64237b`.

| Control | Evidence | Remaining limit |
| --- | --- | --- |
| Secret boundary | DB URL and admin token read only by Node server; package script scans every extension JS file; final `dist/` credential-pattern scan passed | Pattern scan cannot prove absence of every possible secret |
| Scoped auth | Installation token stored as SHA-256 hash, tied to one operator, revocable; claim and action authorization re-read installation revocation and operator enablement after token lookup. Live canary run creation requires an enabled installation owned by the run operator (`2c8de40`). Final authorization for all irreversible actions also rechecks live-canary mode, exact configured QA profile key, and the run's installation. A focused Connect regression and six affected Cockroach integration files passed sequentially on 2026-09-26 | V2 admin bearer token is shared; managed staff identity is needed before rollout |
| Action replay | Unique action target, command idempotency, one-time authorization, expiry, lease uncertainty, reconciliation | Live click race and multi-host concurrency need browser acceptance |
| Remote config | Strict allowlist and bounds for the new viewer-menu and post-detail selectors; no eval/remote JS. Isolated adapter 6 saw temporary config 28 without reload; dev was restored to 27 | Selector effectiveness through an authenticated running extension still needs proof |
| Privacy | Post inspection returns at most five validated LinkedIn URLs and no post text; contact email is accepted only from an observed, identity-matched live canary target and is hidden in admin overview | Contact-email retention and staff access policy still need approval; authenticated UI behavior is unverified |
| Browser permissions | `alarms`, `storage`, `tabs`, exact LinkedIn and V2 API/local host permissions | Final host should be audited after DNS/backend provisioning |
| API browser origins | Staging startup requires an exact HTTPS web origin and a 32-character Chrome extension ID origin. A focused server test accepted only the configured web/extension origins and denied another extension ID and local web origin; local mode is loopback-bound | CORS is an additional browser boundary, not a substitute for token auth; the deployed extension ID and managed admin auth remain to be configured |

Web UI inserts server data through `textContent`, not HTML. Admin token remains in page memory. CSP and exact staging origin controls are present. This is a source-level review, not penetration testing.

The 2.5 extension build scan found no DB URL, admin token, private key marker, `eval`, or `new Function` in packaged JS. The reviewed action target is an exact normalized LinkedIn post URL equal to its command payload; profile and post author identity are checked again in the content primitive. Read-only browser research returned selector counts and profile keys only, with no page content stored in V2 facts. No authenticated submit behavior or managed admin access has been validated.

`pnpm audit --prod --audit-level high` returned "No known vulnerabilities found" on 2026-09-26 for the locked production dependencies. This registry check does not establish that the application or deployed infrastructure is free of vulnerabilities.

The support diagnostic API now returns only selected browser booleans and an email-present flag alongside workflow metadata. A Cockroach regression stored a test contact email in an observation and confirmed that the diagnostic response omitted the address. The broader admin overview still requires managed staff identity and a retention/access policy before rollout.

## Follow-up review: migration and version boundary

Migration `005_global_action_targets` added a V2-only primary key on `(action_type,target_key)` and backfilled historical target keys. The control plane now claims that guard in the same transaction as a new Connect, comment or withdrawal intent. A concurrent Cockroach test used two operators and different lead IDs for one profile and observed one reservation, intent and action command. Direct writes to `action_intents` bypass the service guard, so production action creation must remain behind the control plane; V1/V2 ownership is still not atomic.

Installation issue and remote-config minimum versions now require exact, bounded V2 version strings. The focused test rejects suffixes, leading zeros, oversized components and the wrong major version, and accepts `2.5.0`. This closes a compatibility-input gap; it does not attest that an operator's reported extension build actually matches the installed package.

A local scan of all 12 `dist` files found no exact occurrence of the configured Cockroach URL. The local environment did not contain `V2_ADMIN_TOKEN`, so its value could not be compared with the package. Neither secret variable name appears in the web/extension source or distribution. The server returns a newly issued installation or rotated token only to the authorized admin call, while the admin page displays it until the page closes. Public staging TLS, a deployed-package review, managed staff identity and an operational token handoff procedure remain open.

The Connect and withdrawal actor gap found in the follow-up review is now closed in V2 source version 2.5.1 / adapter 7. A live canary requires a configured actor and the current extension version. Profile and sent-invitation inspections report `viewerMatched` from the browser's signed-in profile or `Me` menu. The server binds that fact to the installation, inspection payload, target, config, and recent observation before reserving or authorizing an action; it also requires viewer evidence for confirmation and reconciliation. The content primitive checks again before the Connect Send or withdrawal confirmation click. Missing or different actors fail closed with `VIEWER_MISMATCH`. Fixture and Cockroach tests cover the new boundary, but authenticated observations through the running 2.5.1 extension and any live acceptance are still pending. No live LinkedIn action has been attempted.

Installation tokens can now be rotated through the admin API without changing installation or run IDs. The server stores only the replacement token's SHA-256 hash, returns the token once with `no-store`, and emits an audit event without token material. Claim, authorization, and ACK recheck the hash so an installation object authenticated before rotation cannot continue acting afterward. Focused Cockroach and HTTP tests verified old-token rejection, repeated rotation, preserved installation ID, admin-route authentication, and refusal to rotate a revoked installation. Managed admin identity and an operational token handoff procedure remain open before rollout.

Package review of local build `f47af6c83434` found 12 distribution files and extension 2.5.1 / adapter 7. The manifest requests only `alarms`, `storage`, and `tabs`, with exact LinkedIn, local API, and V2 API hosts. A package scan found no `COCKROACH_DATABASE_URL`, `V2_ADMIN_TOKEN`, private-key marker, `eval(`, or `new Function` occurrence; an exact comparison also found no copy of the configured Cockroach URL. This is local-package evidence only. The current Chrome profile has not run this package, and the public staging package and HTTPS service remain unverified.

Extension source 2.5.2 adds a bounded local failure receipt buffer. It stores only a random report ID, allowlisted stage/code, optional command UUID, and timestamp; it never stores a token, URL, page HTML, or arbitrary error text in a receipt. The server validates the current installation token and command ownership again, ignores extra request fields, records only allowlisted fields, deduplicates by report ID, and caps accepted reports per installation. The admin event view selects the stage/time explicitly rather than returning raw event details. Worker and Cockroach-backed local HTTP tests passed; this source review does not establish running-Chrome behavior, managed staff access, or an approved receipt retention period.

The local 2.5.2 build from `bc97f36091b2` contains 12 distribution files. Its manifest requests `alarms`, `storage`, and `tabs` with exact LinkedIn, loopback API, and `api-v2.careeraccelerator.net` host patterns. The package scan found no `COCKROACH_DATABASE_URL`, `V2_ADMIN_TOKEN`, private-key marker, `eval(`, or `new Function` occurrence; the configured Cockroach URL was also absent. The isolated Chrome lifecycle passed with no popup console errors. These checks do not establish authenticated use, public deployment security, or absence of every possible secret.

The V2 API now checks admin or installation credentials before parsing a POST body. Authenticated malformed JSON returns `INVALID_JSON` with HTTP 400, and non-object JSON returns `BODY_INVALID` with HTTP 400; the 32 KiB body cap remains. A focused Cockroach-backed local staging-server test passed unauthorized malformed admin and installation requests (HTTP 401), authenticated malformed and non-object requests (HTTP 400), exact-origin allow/deny cases, and a valid admin-create/installation-claim path. The generated V2 test operator was disabled afterward. This avoids treating invalid client input as a server failure, but does not replace managed staff identity, rate controls, or deployed API testing.

Operator enable/disable, installation revoke, and kill-switch changes now write an allowlisted V2 audit event in the same Cockroach transaction as the state change. Missing boolean values are rejected rather than treated as a request to enable; repeated no-op requests add no duplicate event. A focused Cockroach test passed 1/1 for those transitions, old-token rejection, refusal to rotate a revoked installation, and absence of the token from stored and admin-visible events. The existing run state-control integration passed 1/1 after this change. The admin event view selects a validated flag key and state without returning raw event details. The installation form now prompts manual entry of the V2 popup version and build SHA instead of pre-filling an older build; the server cannot attest that the entered values match the installed package. These records show what changed, but the shared admin token still cannot identify which staff member made the change; managed identity remains required before rollout.

Pay-rule creation and enable/disable now use transactional V2 audit events and explicit safe-number/boolean input checks. A focused Cockroach-backed local HTTP test passed rule creation, duplicate/invalid rejection, reversible status changes, sanitized admin visibility, and zero pay rows for a zero-amount fixture. The admin view shows current rule state and selected audit version/state fields without raw event details. Neither this control nor a shared admin token is evidence of an approved rate or an identified human approver; positive compensation and payroll governance remain open.
