# Remote config validation

Environment: V2 Cockroach schema and a running unpacked V2 extension in an isolated Chrome session. Baseline SHA: `68f5971a5e4b91d0814cd0f2b32dd18dbd64237b`.

`pnpm test` rejects unknown config fields, executable-like selectors, unbounded waits and unsafe labels. `V2_TEST_DB=1 node --env-file=<local server env> --test tests/remote-config.integration.test.mjs` passed draft activation, pending-command version update, rollback and operator kill, including resumption after the flag cleared. An MCP shadow smoke changed dev config from version 1 to 3: the same running extension reported version 3 and completed a new observation command without rebuild or reload. The script rolled dev back to version 1. After the authenticated read-only H2 layout finding, `node --env-file=<local server env> scripts/activate-default-config.mjs` activated current source selectors as **dev config version 6**; stable remains version 1. Earlier test configs remain for audit.

This proves server-to-running-extension delivery and schema-checked data loading. The isolated browser redirected to LinkedIn's authwall, so the changed selector's effectiveness on an authenticated target card remains unproven. Config data contains no remote JavaScript or executable DSL. Global/connection/comment/withdrawal/contact, operator, cohort, version and config kill keys are represented server-side; only the implemented action command is currently issued by the workflow.

A 2026-09-26 claim-path follow-up proved a scoped withdrawal flag leaves later eligible profile inspection work claimable; clearing the flag restored the earlier pending invitation inspection. The original remote-config integration fixture used extension 2.5.1 even though new drafts require 2.5.2, so it initially failed with `CONFIG_INCOMPATIBLE` before the changed claim path. The fixture now uses the current source extension version and cleans up its V2 runs/installation/operator; its focused Cockroach rerun passed. The active fixture left by the initial failure was separately paused, revoked and disabled. Type-scoped flags are filtered during candidate discovery, while the final transactional flag check still enforces global, operator, cohort, extension and config flags.

After adding optional post/contact selector families, `activate-default-config.mjs` activated **dev config 10**, minimum extension `2.1.0`; stable remains on config 1. Cockroach rejected a 2.0.0 installation against the dev config. The running 2.1.0 extension reported config 10 in the final shadow smoke. The target layout remains unverified behind LinkedIn authwall.

After adding invitation-manager selector families, the same script activated **dev config 11**, minimum extension `2.2.0`; stable remained on config 1. Cockroach rejected a 2.1.0 installation against the 2.2.0 test config. The running 2.2.0 extension reported config 11 in the invitation shadow smoke. Sent-page selectors remained unverified behind LinkedIn authwall. At that stage the `withdrawal` flag blocked and released only the read-only invitation command.

After adding declarative withdrawal-dialog and confirmation selectors, `activate-default-config.mjs` activated **dev config 15**, minimum extension `2.3.0`; stable and canary remain on config 1. The 2.3.0 extension reported config 15 in the read-only shadow smoke. The `withdrawal` flag also blocked a claimed action at the server authorization boundary in Cockroach. Real dialog selectors are unverified behind LinkedIn authwall.

Comment author, viewer, item, editor and submit selectors were added as bounded data. `activate-default-config.mjs` activated **dev config 24**, minimum extension `2.4.0`; stable and canary remain on config 1. The running 2.4.0 shadow extension reported config 24. `comment` blocks both inspection and action claim, and authorization rechecks it. Authenticated selector effectiveness is unverified.

Read-only authenticated DOM inspection exposed changed viewer and Activity post markup. Adapter 6 adds bounded `viewerMenuTrigger`, `viewerMenu`, `viewerMenuProfile`, `postDetailScope`, and `commentOptionButton` selectors plus viewer and submit labels. `activate-default-config.mjs` activated **dev config 27**, minimum extension `2.5.0`, after the 2.5 code and unit/integration checks. A database readback confirmed an exact match to `DEFAULT_CONFIG`; stable and canary still point to config 1. The new selectors have read-only authenticated DOM evidence, while an authenticated running-extension observation and submit control remain unverified.

An isolated 2.5.0 extension shadow run `f1af7659-88d1-44cd-ac5c-13b719b3f531` saw dev config 27. The smoke script activated temporary selector hotfix config 28; the same running extension observed version 28 and completed its command without a reload. Cleanup restored dev to 27, confirmed by a database readback. Stable and canary stayed on config 1 throughout.
