# Readiness decision

A 2026-09-26 real-catalog shadow workflow completed 20 claim/ACK cycles across two concurrent V2-only runs. The end-to-end script, including setup and final checks, took 121.6 s (about 0.16 completed leads/s overall). All 20 leads completed with one simulated observation each; actions, intents, and pay remained zero, and V1 assignment count stayed 63,135. This strengthens state-machine and isolation evidence, but does not establish 1k/10k transactional throughput or authenticated browser behavior. See [SHADOW-LOAD.md](SHADOW-LOAD.md).

**NOT PROD-READY-CANDIDATE** as of 2026-09-26. Starting main SHA: `68f5971a5e4b91d0814cd0f2b32dd18dbd64237b`. V1-overlap implementation commit: `2c8de40`; last reviewed local package build SHA: `f47af6c83434`. Protocol `1`; V2 source extension `2.5.1`; adapter `7`; dev remote config `27`; DB migrations `001_init` through `005_global_action_targets`.

The local V2 architecture, migration, protocol fixtures, Cockroach action reconciliation, remote config update, Chrome MCP lifecycle, 1k/10k real-catalog shadow workloads, and local API smoke have evidence in sibling validation files. These results do not substitute for all 15 gates in the context kit.

Branch-scoped [V2 GitHub Actions run 36171523056](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36171523056) passed tests, build, and V2-only FTP upload on the recovery batch. Public V2 web/API DNS still has no A records, and no dedicated backend is deployed.

Read-only comment-state, canary-scoped contact-email, and sent-invitation inspection have fixture and Cockroach tests. A QA-scoped `EXECUTE_WITHDRAW` path now requires a same-installation, eligible observation from the last two minutes, one-time server authorization, a fresh browser identity/age check, one confirmation click, and visible card removal. Cockroach tests passed duplicate ACK, kill-before-authorization, lost lease, observation-only reconciliation, late ACK, and zero-pay cases. No real LinkedIn withdrawal was attempted. The 2.3.0 Chrome shadow run `45885c19-0995-4945-ad4f-78a18f04f769` proved read-only invitation routing with config 15 but hit LinkedIn authwall.

The reviewed QA comment path now has V2-only draft/review state, an actor-bound post inspection, one-time authorization, a single submit primitive and observation-only recovery. Cockroach verified direct and reconciled confirmation, late ACK, duplicate prevention and a single zero-amount fixture pay entry; simulated DOM fixtures cover author/viewer identity and same-text comments by another person. The 2.4.0 Chrome lifecycle and read-only shadow smoke passed, but the isolated browser was redirected to LinkedIn authwall. See [COMMENT.md](COMMENT.md).

The recovery batch at `a142581` added a real V2 backend process-stop/restart integration: a previously authorized lost-ACK Connect lease became one observation on the fresh server process, with no second action command. A late uncertain ACK after reconciliation no longer reopens a confirmed connection. Worker fixtures covered restart after ACK network loss and authorization outage before a primitive. Cockroach checked pause/resume, safe reinspection of a paused read-only lead, operator/global kills, daily-limit attribution, and rejection of Connect while another action intent is unresolved. The withdrawal queue now uses the current 2.4 minimum config; its 2.4 Cockroach integration passed. Package `a142581d708c` passed Chrome DevTools MCP install/list/reload/popup/service-worker/uninstall with no popup errors. Public V2 DNS remains absent at the latest check.

The 2.5 candidate in this branch responds to read-only authenticated DOM research: viewer identity through the `Me` menu, role-based Activity post cards, conservative repost exclusion, and approved comment commands routed to the post detail URL. Cockroach tests passed the reviewed comment route, a daily-cap concurrency race, a Connect-versus-comment race, and cross-operator/revocation checks. Unit tests and the V1 integrity check passed. Dev config 27 requires extension 2.5.0; stable and canary remain on config 1. Authenticated behavior through the running extension remains unverified. See [AUTHENTICATED-DOM.md](AUTHENTICATED-DOM.md).

The isolated 2.5.0 package passed Chrome DevTools MCP install/list/reload/trigger/popup/service-worker/uninstall with no popup errors. Read-only shadow commands reached the backend under config 27 and safely stopped at LinkedIn authwall. A temporary config 28 selector hotfix was seen by the same running extension without reload, then dev was restored to 27. Branch [CI run 36174795017](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36174795017) passed tests, build, and V2-only static FTP sync; DNS lookup still found no A records for the V2 web/API hostnames.

The V2-only live canary now reads `public.lead_assignments` at run creation and action authorization and rejects a lead already assigned in V1 (`57179fa`). It also requires an enabled installation owned by the run's operator (`2c8de40`), which the admin run form now accepts. The ownership check is a read-only overlap filter; V1 can still assign between the check and V2 action. Local `pnpm test` passed 38 tests with 11 DB-gated skips; a configured Cockroach run then passed the new ownership regression and eight affected action/recovery integrations with no skips. The post-test V1 assignment count was 63,135 and dev config remained 27. Local `pnpm build` and `pnpm verify:v1` passed. [Branch CI run 36177796073](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36177796073) passed tests, build, and V2-only static FTP sync. See [the ownership design](../lead-ownership-cutover.md) for the required joint cutover protocol.

An additional V2-only transaction test at `95b835c` injected SQLSTATE `40001` after a pending event insert. Cockroach rollback/retry left exactly one committed event and the callback ran twice. The first attempt at running this test hit a transient pool connection timeout before the transaction; a read-only probe and the focused rerun succeeded. This validates the injected retry path, not a naturally concurrent conflict. After adding it, local `pnpm test` passed 38 with 12 DB-gated skips and `pnpm verify:v1` passed.

A separate focused Cockroach test at `1df8cd6` induced an actual concurrent update conflict on one V2 test operator row. The stale transaction retried, and both increments survived in the final value 3. The local suite then passed 38 tests with 13 DB-gated skips, and `pnpm verify:v1` passed. Browser click/restart timing remains untested.

Two focused background-worker VM cases simulated a tab closing and a page refresh after action dispatch by rejecting the Chrome message channel. Both produced an `uncertain` ACK and the simulated next poll did not dispatch the action again; `node --test tests/background.test.mjs` passed 9/9. Existing Cockroach tests separately cover action-lease reconciliation. These fixtures do not establish real Chrome tab-close or refresh timing during a click.

A focused Cockroach test passed 1/1 with two control-plane instances using separate DB pools: parallel claims produced one inspection lease and one action lease; parallel authorization accepted once; lease-expiry recovery produced one read-only reconciliation and no second action command or attempt. Both instances ran in one Node process, and the test did not force a specific lock interleaving or simulate an actual browser click.

The staging API now rejects unconfigured browser origins: staging startup requires an exact HTTPS web origin and Chrome extension ID, while local mode cannot bind beyond loopback. Origin-policy unit tests passed 2/2, and a Cockroach-backed local staging server test passed 1/1 with allowed web/extension origins and three denied origins. This hardens browser access but does not provide managed staff identity or a public API deployment.

Protocol regression tests now reject malformed percent escapes and encoded separators in LinkedIn profile URLs before V2 run selection or command creation. The focused protocol suite passed 4/4. This is a parser safeguard, not a fresh full-catalog shadow run.

The backend now marks a run `completed` and emits one completion event in the final lead ACK transaction, after confirming no unfinished leads, active commands, or unresolved intents remain. A focused Cockroach completion test passed 1/1, including a queued-command guard. The affected integration run passed 8/9 initially; withdrawal's duplicate error changed after run completion, so the existing-intent check was restored before the running-run check and its focused rerun passed 1/1. A final-code real-catalog shadow workflow completed two concurrent runs with two claim/ACKs each, zero actions/intents/pay, and unchanged V1 assignment count of 63,135. It took 38.7 s for four leads (about 0.1/s), which does not establish production transaction throughput. See [SHADOW-LOAD.md](SHADOW-LOAD.md) and [DATABASE.md](DATABASE.md).

Post-completion requests now reopen a run for an explicit read-only inspection or a fresh, eligible QA comment/withdrawal. Two repeated inspection cycles passed the focused Cockroach completion test on the final code; completed-run withdrawal and comment fixtures passed their focused integrations before the shared helper's final config-version update. No real action was made. The new command updates the run's config version, and each reopen/completion cycle has its own event. This removes the admin dead end after an initial no-op observation without changing the remaining browser and deployment gates.

The final Connect authorization boundary now rechecks live-canary mode, the exact configured QA profile key, and the run's installation, as comment and withdrawal already did. A focused Cockroach regression denied synthetic commands with each mismatch and authorized exactly once only when all scopes matched. Seven affected cases across authorization, backend restart, Connect lease/pay, two-instance authorization, comment, and withdrawal passed sequentially with no skips or failures. Local `pnpm test` passed 42 checks with 17 DB-gated skips; `pnpm verify:v1` passed. No LinkedIn action was attempted.

Support diagnostics now expose correlated run/lead/command and installation IDs, build/config versions, command and lead state, attempts, intent/reconciliation state, and allowlisted browser facts. A focused Cockroach support test passed 1/1 and demonstrated that a stored test contact email is absent from the diagnostic response. The affected Connect, comment, and withdrawal integrations passed sequentially 3/3. Local `pnpm test` passed 42 checks with 18 DB-gated skips; `pnpm verify:v1` passed. See [SUPPORT.md](SUPPORT.md). This improves backend support visibility; a browser or backend outage before a result reaches V2 and managed staff access are still open.

A V2-only global action target guard now prevents a second Connect, comment or withdrawal reservation for the same profile or post across operators and lead IDs. Migration `005` preserved historical intent rows and passed a repeat invocation. A concurrent Cockroach Connect race across two operators left one target reservation, one intent and one action command; eight other focused Cockroach cases passed sequentially. Local `pnpm test` passed 43 with 19 DB-gated skips, and `pnpm verify:v1` passed. Historical duplicate synthetic QA intents remain as evidence; the guard blocks new V2 reservations. No live LinkedIn click or shared V1/V2 ownership was tested by this change.

The full five-migration chain also passed two applications on a temporary empty Cockroach database: five ledger rows, 19 V2 tables and the action-target primary key were verified before cleanup. A source/package security review found no configured database URL in 12 local distribution files and no secret variable names in web/extension artifacts. Installation and config version inputs now reject malformed or oversized V2 version strings; the focused regression passed. Local `pnpm test` passed 44 with 19 DB-gated skips, and `pnpm verify:v1` passed. See [SECURITY.md](SECURITY.md). Public staging TLS, managed staff identity, deployed-package secret verification and authenticated browser validation remain open.

A retry in the selected current Chrome profile reached an authenticated LinkedIn feed in a fresh tab and verified that the `Me` menu and sidebar resolve to the same signed-in profile. Chrome's internal Extensions tab was not controllable through that browser bridge, so manual unpacked installation was requested. The local V2 package was rebuilt from `dc43acd612f4` for that handoff. No running-extension command or LinkedIn action occurred; the authenticated extension gate remains open. See [AUTHENTICATED-DOM.md](AUTHENTICATED-DOM.md).

A follow-up authorization review found that Connect and withdrawal verified the intended recipient without binding a live action to the signed-in LinkedIn actor. V2 source 2.5.1 / adapter 7 now requires an actor-bound profile or invitation inspection, server-side source checks at authorization, viewer evidence at confirmation, and a browser recheck before the final click. Local adapter cases and focused Cockroach authorization, lost-ACK, withdrawal, support, state-control, and concurrency cases passed; authenticated behavior through the running 2.5.1 extension remains unverified. A read-only Cockroach index audit confirmed the expired-command recovery plan scans only an operator's pending/leased command spans, despite 11,501 completed commands in this test catalog. See [SECURITY.md](SECURITY.md) and [DATABASE.md](DATABASE.md).

The V2 backend now supports in-place installation-token rotation through the admin API. Focused Cockroach and HTTP tests verified that old and stale-server tokens stop authenticating, a replacement keeps the same installation ID, repeated rotation invalidates the prior replacement, the route requires admin auth, and revoked installations refuse rotation. Audit events contain no token. This closes the source-level rotatability requirement; managed admin identity, secure operator handoff and deployed operation remain unverified.

Config-related command-claim failures now produce a bounded `claim_rejected` event for support before any browser command exists. A Cockroach test verified an incompatible extension version records the operator, installation, extension/build/config versions and code, while duplicate polls within the hour create one event. The admin event view exposes these fields. Browser-side outages before reaching the backend still cannot create a server event.

Local package `f47af6c83434` passed a 12-file distribution review: extension version and adapter metadata match the manifest, permissions are limited to alarms/storage/tabs plus exact LinkedIn and V2 API/local hosts, secret variable names and executable-code markers are absent, and the configured Cockroach URL does not appear in the package. The selected Chrome profile still does not expose its internal Extensions tab or a DevTools debugging endpoint to the bridge; no 2.5.1 running-extension observation was verified. See [SECURITY.md](SECURITY.md) and [AUTHENTICATED-DOM.md](AUTHENTICATED-DOM.md).

Remaining gates include: authenticated no-op observations through the *running V2 extension* on multiple real profile states, posts and invitation-manager cards; one explicitly consenting QA recipient and exactly-one live acceptance; broader crash/network/concurrency injections; authenticated comment and withdrawal validation; approved pay policy; V2 DNS/backend/web staging deployment and managed admin auth; final package/security review. The authoritative DNS is on SiteGround, but the current browser profile reaches only its login page; no DNS change was attempted. Mapping the V2 web hostname directly to the existing V1/FTP IP returned SiteGround's default virtual host on HTTP and failed HTTPS certificate-name validation, so the successful FTP sync still does not prove public V2 web serving. V1/V2 shared lead ownership remains a separate cutover prerequisite. Action paths have explicit `not_submitted` outcomes and observation-only recovery after uncertainty. No cutover, main merge, V1 deployment or production extension replacement is authorized by this branch.
