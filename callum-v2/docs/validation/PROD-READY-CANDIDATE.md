# Readiness decision

**NOT PROD-READY-CANDIDATE** as of 2026-09-25. Starting main SHA: `68f5971a5e4b91d0814cd0f2b32dd18dbd64237b`. Current control-plane implementation commit: `2c8de40`; latest local package build SHA: `d6098d5513ff` (extension source unchanged since `8b04b5c7f891`). Protocol `1`; extension `2.5.0`; adapter `6`; dev remote config `27`; DB migrations `001_init` through `004_comment_action`.

The local V2 architecture, migration, protocol fixtures, Cockroach action reconciliation, remote config update, Chrome MCP lifecycle, 1k/10k real-catalog shadow workloads, and local API smoke have evidence in sibling validation files. These results do not substitute for all 15 gates in the context kit.

Branch-scoped [V2 GitHub Actions run 36171523056](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36171523056) passed tests, build, and V2-only FTP upload on the recovery batch. Public V2 web/API DNS still has no A records, and no dedicated backend is deployed.

Read-only comment-state, canary-scoped contact-email, and sent-invitation inspection have fixture and Cockroach tests. A QA-scoped `EXECUTE_WITHDRAW` path now requires a same-installation, eligible observation from the last two minutes, one-time server authorization, a fresh browser identity/age check, one confirmation click, and visible card removal. Cockroach tests passed duplicate ACK, kill-before-authorization, lost lease, observation-only reconciliation, late ACK, and zero-pay cases. No real LinkedIn withdrawal was attempted. The 2.3.0 Chrome shadow run `45885c19-0995-4945-ad4f-78a18f04f769` proved read-only invitation routing with config 15 but hit LinkedIn authwall.

The reviewed QA comment path now has V2-only draft/review state, an actor-bound post inspection, one-time authorization, a single submit primitive and observation-only recovery. Cockroach verified direct and reconciled confirmation, late ACK, duplicate prevention and a single zero-amount fixture pay entry; simulated DOM fixtures cover author/viewer identity and same-text comments by another person. The 2.4.0 Chrome lifecycle and read-only shadow smoke passed, but the isolated browser was redirected to LinkedIn authwall. See [COMMENT.md](COMMENT.md).

The recovery batch at `a142581` added a real V2 backend process-stop/restart integration: a previously authorized lost-ACK Connect lease became one observation on the fresh server process, with no second action command. A late uncertain ACK after reconciliation no longer reopens a confirmed connection. Worker fixtures covered restart after ACK network loss and authorization outage before a primitive. Cockroach checked pause/resume, safe reinspection of a paused read-only lead, operator/global kills, daily-limit attribution, and rejection of Connect while another action intent is unresolved. The withdrawal queue now uses the current 2.4 minimum config; its 2.4 Cockroach integration passed. Package `a142581d708c` passed Chrome DevTools MCP install/list/reload/popup/service-worker/uninstall with no popup errors. Public V2 DNS remains absent at the latest check.

The 2.5 candidate in this branch responds to read-only authenticated DOM research: viewer identity through the `Me` menu, role-based Activity post cards, conservative repost exclusion, and approved comment commands routed to the post detail URL. Cockroach tests passed the reviewed comment route, a daily-cap concurrency race, a Connect-versus-comment race, and cross-operator/revocation checks. Unit tests and the V1 integrity check passed. Dev config 27 requires extension 2.5.0; stable and canary remain on config 1. Authenticated behavior through the running extension remains unverified. See [AUTHENTICATED-DOM.md](AUTHENTICATED-DOM.md).

The isolated 2.5.0 package passed Chrome DevTools MCP install/list/reload/trigger/popup/service-worker/uninstall with no popup errors. Read-only shadow commands reached the backend under config 27 and safely stopped at LinkedIn authwall. A temporary config 28 selector hotfix was seen by the same running extension without reload, then dev was restored to 27. Branch [CI run 36174795017](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36174795017) passed tests, build, and V2-only static FTP sync; DNS lookup still found no A records for the V2 web/API hostnames.

The V2-only live canary now reads `public.lead_assignments` at run creation and action authorization and rejects a lead already assigned in V1 (`57179fa`). It also requires an enabled installation owned by the run's operator (`2c8de40`), which the admin run form now accepts. The ownership check is a read-only overlap filter; V1 can still assign between the check and V2 action. Local `pnpm test` passed 38 tests with 11 DB-gated skips; a configured Cockroach run then passed the new ownership regression and eight affected action/recovery integrations with no skips. The post-test V1 assignment count was 63,135 and dev config remained 27. Local `pnpm build` and `pnpm verify:v1` passed. [Branch CI run 36177796073](https://github.com/codeblix-ltd/CALLUM_SalesNavigator/actions/runs/36177796073) passed tests, build, and V2-only static FTP sync. See [the ownership design](../lead-ownership-cutover.md) for the required joint cutover protocol.

Remaining gates include: authenticated no-op observations through the *running V2 extension* on multiple real profile states, posts and invitation-manager cards; one explicitly consenting QA recipient and exactly-one live acceptance; broader crash/network/concurrency injections; authenticated comment and withdrawal validation; approved pay policy; V2 DNS/backend/web staging deployment and managed admin auth; final package/security review. V1/V2 shared lead ownership remains a separate cutover prerequisite. Action paths have explicit `not_submitted` outcomes and observation-only recovery after uncertainty. No cutover, main merge, V1 deployment or production extension replacement is authorized by this branch.
